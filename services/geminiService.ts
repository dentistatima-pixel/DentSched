
// This service has been disabled as AI features are paid and removed.
export const chatWithAssistant = async () => {
  return "AI features are disabled.";
};

export const summarizePatient = async () => {
    return "AI features are disabled.";
}
